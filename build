pipeline {
  agent {
    label 'Windows Slave 1'
  }
  stages {
    stage('Build') {
      steps {
        dir('src') {
           bat 'dotnet build --configuration Release'
        }
      }
    }
  }
}
